spring:
  datasource:
    url: jdbc:sqlite:file::memory:?cache=shared
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

app:
  jwt:
    secret: test-secret-key-for-unit-tests-only-32c
    expiration-ms: 3600000
