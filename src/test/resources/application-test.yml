spring:
  datasource:
    url: jdbc:sqlite:file::memory:?cache=shared
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

application:
  security:
    jwt:
      secret-key: test-secret-key-for-unit-tests-only-32c
      expiration: 3600000
